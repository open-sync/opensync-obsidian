{
  "name": "opensync-obsidian",
  "version": "0.1.0",
  "description": "Free, end-to-end encrypted sync for Obsidian vaults.",
  "main": "main.js",
  "scripts": {
    "wasm": "npm --prefix ../opensync/packages/client run wasm && npm run vendor",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "check:obsidian": "OPENSYNC_HOSTED_RELAY= node scripts/build.mjs && node checks/obsidiancheck.mjs",
    "shots": "node checks/capture.mjs docs/screenshots",
    "check:tiers": "OPENSYNC_HOSTED_RELAY= node scripts/build.mjs && node checks/tierscheck.mjs",
    "check:hosted": "node scripts/build.mjs && node checks/hostedcheck.mjs",
    "check:all": "npm run vendor:check && npm run check:obsidian && npm run check:tiers && npm run check:hosted",
    "vendor": "node scripts/vendor-client.mjs",
    "vendor:check": "node scripts/vendor-client.mjs --check"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "playwright-core": "^1.63.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0"
  },
  "//": "The client library is imported by relative path and bundled by esbuild; it is not published."
}
